{
  "_description": "Gateway registers its integrations and tools, service acknowledges",
  "gateway_sends": {
    "type": "register",
    "integrations": [
      {
        "type": "mcp_server",
        "id": "postgresql",
        "name": "PostgreSQL",
        "description": "Query and inspect PostgreSQL databases",
        "tools": [
          {
            "name": "query",
            "description": "Execute a read-only SQL query",
            "inputSchema": {
              "type": "object",
              "properties": {
                "sql": { "type": "string", "description": "SQL query to execute" }
              },
              "required": ["sql"]
            }
          },
          {
            "name": "list_tables",
            "description": "List all tables in the database",
            "inputSchema": {
              "type": "object",
              "properties": {},
              "required": []
            }
          }
        ]
      }
    ]
  },
  "service_responds": {
    "type": "registered",
    "integrationCount": 1,
    "toolCount": 2
  }
}
