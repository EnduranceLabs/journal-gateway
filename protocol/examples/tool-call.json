{
  "_description": "Service sends a tool_call, gateway executes and returns tool_result",
  "service_sends": {
    "type": "tool_call",
    "requestId": "req_abc123",
    "skillId": "postgresql",
    "toolName": "query",
    "arguments": {
      "sql": "SELECT count(*) FROM users WHERE created_at > now() - interval '24 hours'"
    }
  },
  "gateway_responds": {
    "type": "tool_result",
    "requestId": "req_abc123",
    "result": {
      "content": [
        {
          "type": "text",
          "text": "[{\"count\": 42}]"
        }
      ]
    }
  }
}
