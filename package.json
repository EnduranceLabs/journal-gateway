{
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@10.15.1",
  "scripts": {
    "build": "cd gateway && pnpm build",
    "build:client": "cd clients/typescript && pnpm install && pnpm build",
    "test": "cd gateway && pnpm test",
    "test:client": "cd clients/typescript && pnpm install && pnpm test",
    "test:integration": "cd testing/integration/ts && pnpm install && pnpm test",
    "typecheck": "cd gateway && pnpm typecheck"
  }
}
