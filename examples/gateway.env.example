JOURNAL_GATEWAY_TOKEN=gw_demo
JOURNAL_GATEWAY_URL=ws://localhost:8080

# PostgreSQL example used by gateway.json.
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DATABASE=analytics
POSTGRES_USER=journal_gateway_ro
POSTGRES_PASSWORD=replace-me

# Remote API example used by gateway.json.
# Include the full header value expected by the remote MCP server.
REMOTE_MCP_AUTHORIZATION=Bearer replace-me
